# CLAUDE.md

- Priorizar cambios pequeños y verificables por commit.
- Antes de tocar scraping: actualizar matriz legal y dejar la fuente detrás de flag.
- Evitar romper endpoints existentes (`/api/geo/*`, `/api/monitor/*`).
- Ejecutar `npm run build` y `npm run test` antes de finalizar.
