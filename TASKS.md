# TASKS

## MVP (0-90 días)

1. Ingesta RSS institucional + store + endpoints (`owner: ingestion/data`).
2. GeoRef normalization wrapper y score de confianza (`owner: geo`).
3. UI mapa + panel lateral + filtros (`owner: ui`).
4. Scheduler y trigger manual (`owner: api`).

## 90-180 días

1. CKAN robusto (CSV/XLSX/PDF parsing incremental).
2. Dedupe semántico y evidencia multi-fuente.
3. Alertas por watchlist de zona/tipo.

## 180-365 días

1. Integración SIEM / sistemas institucionales.
2. Gobernanza formal de licencias y privacy by design.
3. Forecasting y análisis predictivo.
