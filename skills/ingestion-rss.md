# skill: ingestion-rss

- Confirm legal matrix entry exists.
- Use timeout + user-agent + retry budget.
- Store metadata/snippet only when license is uncertain.
- Emit normalized `source_item` records.
